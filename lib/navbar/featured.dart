import 'package:flutter/material.dart';

class Featured extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Featured Page Content'),
    );
  }
}
